- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc: zshrc
   ~/.gitconfig: gitconfig
   ~/.functions: functions

- create:
   - ~/Projects

# - shell:
# 	- [git submodule update --init --recursive, Installing submodules]
