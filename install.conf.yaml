- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#  ~/.vim: vim
   ~/.zshrc: zshrc
   ~/.gitconfig: gitconfig
   ~/.functions: functions

- create:
   - ~/Projects

# - shell:
# 	- [git submodule update --init --recursive, Installing submodules]
